{% if mysql_syslog_use %}
module(load="imfile" PollingInterval="10")

input(type="imfile"
      File="{{ mysql_log_error }}"
      Tag="mysql:"
      Severity="error"
      Facility="local7"
      startmsg.regex="[0-9]{4}-[0-9]{2}-[0-9]{2}")
{% endif %}
