global:
  scrape_interval: {{ vmagent_scrape_interval }}
scrape_configs:
- job_name: {{ vmagent_job_name }}
  static_configs:
  - targets:
{% for host in groups['nova_compute'] %}
    - {{ hostvars[host].ip.mgmt }}:9100
{% endfor %}
